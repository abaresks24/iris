{
  "name": "iris",
  "version": "0.1.0",
  "private": true,
  "description": "Iris — cross-chain options on top of Derive's permissionless orderbook (ETHGlobal NYC)",
  "scripts": {
    "dev": "npm run dev --prefix server & npm run dev --prefix web",
    "dev:server": "npm run dev --prefix server",
    "dev:web": "npm run dev --prefix web",
    "install:all": "npm install --prefix server && npm install --prefix web"
  }
}
